# Effect v4 SQL

Reference docs for the `sql` pillar (`effect/unstable/sql` + driver packages).

- [Client and Queries](client-and-queries.md): driver layers (pg, bun-sqlite, sqlite-node), the `sql` tagged template, helpers, statement channels, transactions, `SqlError`.
- [Schema, Models, Migrations](schema-models-migrations.md): `SqlSchema`, `SqlResolver`, `SqlModel` (`Model.Class` + repository/resolvers), `Migrator`.
