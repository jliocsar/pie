# Schema

Effect v4 `Schema` pillar reference.

- [`schema.md`](./schema.md) — top-level `Schema` import, Struct/Class, type extraction, brands, `.check` filters, `decodeTo` transforms, decode/encode variants, Union/Record/optional combinators, annotations.
