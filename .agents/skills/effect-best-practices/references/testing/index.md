# testing

Effect-aware testing helpers for Effect v4 via `@effect/vitest`.

- [`vitest.md`](./vitest.md) — `it.effect`/`it.live`/`it.layer`/`layer()`, `TestClock` time control, `flakyTest`, and property testing with `prop`.
